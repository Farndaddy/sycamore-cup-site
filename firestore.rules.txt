// Paste this into Firebase Console > Firestore Database > Rules, then Publish.
// This intentionally has NO login system (any of the guys can enter scores from
// their phone with just the site URL) but blocks deleting data and caps how much
// text can be written per field so a stray bot can't trash the database.
//
// 2026-09-08: the app's PIN / card-claim step was removed so anyone can enter a
// score for anyone. THESE RULES DID NOT CHANGE and did not need to — they never
// checked identity in the first place. The PIN was only ever a client-side
// convention about whose card you were on, not security. What protects the data
// is unchanged: scores can be created and updated but never deleted, and every
// edit appends to scoreLog with the original score preserved.

// WARNING (2026-09-09): this file is PARTIAL. The rules actually deployed also
// cover players, rounds, teamScores, scoreLog and admins — collections the app
// writes to every day. Do NOT paste this file over the live rules or you will
// break score entry. Edit the deployed rules in the console instead.
//
// TWO THINGS TO CHECK in the console:
//   1. `strokes < 20` below. The triple-bogey cap was removed from the app on
//      2026-09-08 ("every stroke counts"), but this rule still refuses a 20 or
//      higher. Change it to `< 30` if anyone could card that.
//   2. THE SELF-EDIT WINDOW IS 20 MINUTES as of 2026-09-11, raised from 2.
//      js/live.js SELF_EDIT_MINUTES must equal the rule. Find
//      withinSelfEditWindow() in the deployed rules and make it read
//      duration.value(20, 'm'). Publish the RULE FIRST, then the code — the
//      other order lets the countdown invite an edit the database refuses.
//   3. Reset/restore (added 2026-09-09) works by UPDATING a score with a
//      `voided` flag, never deleting it. The existing update rule already
//      allows it — `allow delete: if false` stays exactly as it is, and should.

rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    match /scores/{scoreId} {
      allow read: if true;
      allow create, update: if
        request.resource.data.strokes is number &&
        request.resource.data.strokes > 0 &&
        request.resource.data.strokes < 20 &&
        request.resource.data.hole is number &&
        request.resource.data.hole > 0 &&
        request.resource.data.hole <= 18;
      allow delete: if false;
    }

    match /courses/{courseId} {
      allow read: if true;
      allow create, update: if request.resource.data.pars is list;
      allow delete: if false;
    }
  }
}

// Later, if you want to restrict WRITES to just your group (not the whole internet),
// the simplest upgrade is Firebase Anonymous Auth + a shared invite link, or a
// single shared "trip password" checked in the app before allowing submitScore().
// Ask Claude to add either of these when you're ready — the data model above
// doesn't need to change.
